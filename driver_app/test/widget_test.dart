import 'package:flutter_test/flutter_test.dart';

import 'package:rideapp_driver/app.dart';

void main() {
  test('DriverApp can be constructed', () {
    expect(const DriverApp(), isA<DriverApp>());
  });
}
